# Create a `movedesign_processed` object for preprocessed results

Creates an S3 object of class `movedesign_processed` to store
preprocessing steps or outputs during a `movedesign` workflow.

## Usage

``` r
movedesign_processed(input)
```

## Arguments

- input:

  A list, containing preprocessed results and metadata.

## Value

An object of class `movedesign_processed` and `movedesign`.

## See also

[`md_run()`](https://ecoisilva.github.io/movedesign/reference/md_run.md)
