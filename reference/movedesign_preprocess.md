# Create a `movedesign_preprocess` object for preprocessed results

Creates an S3 object of class `movedesign_preprocess` to store
preprocessing steps or outputs during a `movedesign` workflow.

## Usage

``` r
movedesign_preprocess(input)
```

## Arguments

- input:

  A list, containing preprocessed results and metadata.

## Value

An object of class `movedesign_preprocess` and `movedesign`.

## See also

[`md_run()`](https://ecoisilva.github.io/movedesign/reference/md_run.md)
