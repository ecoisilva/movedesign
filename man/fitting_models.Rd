% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_sims.R
\name{fitting_models}
\alias{fitting_models}
\title{Fit continuous-time movement models}
\usage{
fitting_models(
  obj,
  set_target = c("hr", "ctsd"),
  parallel = FALSE,
  trace = FALSE,
  ncores = parallel::detectCores(),
  ...
)
}
\arguments{
\item{obj}{A list of simulated movement datasets, each a \code{telemetry}
object compatible with \code{ctmm} \code{R} package.}

\item{set_target}{A character vector specifying the research targets.
Current options:
\describe{
\item{"hr"}{Home range estimation.}
\item{"ctsd"}{Speed & distance estimation.}
}}

\item{parallel}{Logical. If \code{TRUE}, enables parallel processing.}

\item{trace}{Logical. If \code{TRUE} (default), prints progress and
timing messages to the console.}

\item{ncores}{Integer. Number of CPU cores to use for parallel
processing. Defaults to all available cores minus one.}

\item{...}{Additional arguments used internally.}
}
\value{
A list of fitted movement models, all recentered to the origin.
}
\description{
This function fits continuous-time movement models to simulated location
data using the \code{ctmm} package. It estimates movement parameters for each
simulated trajectory, allowing for parallel execution. It currently
supports both home range and speed estimation workflows.
}
\details{
The function generates initial parameter estimates for each dataset
using \code{ctmm::ctmm.guess()}. If the data includes simulated location
error, it adds an error model accordingly. Models are fitted using
\code{ctmm::ctmm.select()}, which performs model selection to find the
best-fit movement process. Finally, all fitted models are recentered
to (\verb{0, 0}) for downstream consistency.
}
\note{
This function is intended for internal use and may assume inputs
follow specific structure and constraints not referenced explicitly.
}
\seealso{
\code{ctmm::ctmm.guess()}, \code{ctmm::ctmm.select()}
}
