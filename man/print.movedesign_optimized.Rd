% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_main.R
\name{print.movedesign_optimized}
\alias{print.movedesign_optimized}
\title{Print method for \code{movedesign_optimized} objects}
\usage{
\method{print}{movedesign_optimized}(
  x,
  verbose = TRUE,
  m = NULL,
  ci = 0.95,
  tol = 0.05,
  n_converge = 9,
  plot = TRUE,
  pal = c("#007d80", "#A12C3B"),
  ...
)
}
\arguments{
\item{x}{An object of class \code{movedesign_optimized}.}

\item{verbose}{Logical. If \code{TRUE}, run \code{\link[=md_check]{md_check()}} and print full
convergence diagnostics. Also displays a convergence plot if
\code{plot = TRUE}. Defaults to \code{FALSE}.}

\item{m}{Numeric (optional). Restricts results to a specific population
sample size. Defaults to \code{NULL}, which uses the maximum sample size.}

\item{ci}{Numeric. Confidence level for intervals (applied to narrow
confidence bars and wide prediction bands). Must be between
\code{0} and \code{1}. Default is \code{0.95}.}

\item{tol}{Numeric. Tolerance threshold for absolute change in
cumulative mean to declare convergence. Default is \code{0.05}.}

\item{n_converge}{Integer. Number of consecutive steps within
tolerance required to confirm convergence. Default is \code{9}.}

\item{plot}{Logical. If \code{TRUE}, generates a convergence plot. Default
is \code{TRUE}.}

\item{pal}{Character vector of colors for the convergence plot, e.g.
\code{c("#007d80", "#A12C3B")}. Default is \code{c("#007d80", "#A12C3B")}.}

\item{...}{Additional arguments (currently unused).}
}
\description{
Print a structured summary of a \code{movedesign_optimized} object produced
by \code{\link[=md_optimize]{md_optimize()}}. This includes study design details, replication
settings, estimation performance per target metric, and an optional
convergence assessment.
}
